# prompts the user to enter their name
name = input("What is your name? ")

# uses the len function to determine the length of the name
length = len(name)

# prints the length of the name
print(length)